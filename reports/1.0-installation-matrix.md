# Axiom 1.0 — Installation Matrix

*Maintainer artifact. Not shipped.* Candidate: `1.0.0-rc.1`. Everything below installs the
**packed RC tarballs** (`release/*.tgz`), never workspace links.

## Declared support

`engines.node = ">=22.0.0"` on all 8 published manifests. Package manager claimed: **npm
only** (no `pnpm`/`yarn`/`bun` claim is made or tested — accuracy over breadth). Module
system: **ESM only** (`"type": "module"`, `exports` exposes an `import` condition and no
`require`). TypeScript: `moduleResolution` `NodeNext`/`Bundler`, `strict` — validated by
compiling every consumer project against the shipped `.d.ts`.

## Matrix

| Environment | install (5 consumer projects) | TS compile | runtime exec | conformance | workspace suite | Result |
| --- | --- | --- | --- | --- | --- | --- |
| **Node 22.23.2**, npm 10.9.8 (lowest supported 22.x available) | 5/5 ok, 0 repo-leak | 5/5 ok | 5/5 ok | 46/46 (via `c5`) | `npm run build` ok · fast tier **1707 / 1707 / 0 fail** | **PASS** |
| **Node 24.19.0**, npm 11.17.0 (current) | 5/5 ok, 0 repo-leak | 5/5 ok | 5/5 ok | 46/46 (via `c5`) | build ok · fast tier **1707 / 1707** · **soak tier 1707 / 1707** (server workspace ~6.5 min incl. `workflow-crash-matrix` @ 50 trials) | **PASS** |
| **Node 23.3.0**, npm 10.9.x (interim, not an LTS — no claim) | 5/5 ok, 0 repo-leak | 5/5 ok | 5/5 ok | 46/46 (via `c5`) | not run | **PASS** (smoke) |

- "repo-leak" = any installed dependency resolving to a path under the monorepo, or any
  `@cynodia/*` at a version other than `1.0.0-rc.1`. Checked via `npm ls --json --all` in
  every project — **0** in every cell.
- The built‑in `scripts/consumer-test.mjs` (installs all 8 tarballs, 32 assertions + the
  installed `axiom` CLI + the materialization gate) is green on Node 24; the discoverability
  probe and `verify-packages` are green.

## Notes

- Node 22 is the floor because `node --test` native glob expansion of `dist-test/**/*.test.js`
  and `node:sqlite` (used lazily by the optional SQLite adapters, with a documented in-memory
  fallback) both require ≥ 22. No Node 20 path is claimed because none is tested.
- Node 23 emits a benign npm‑internal `ExperimentalWarning` (`require()` of an ES module in
  npm's own `supports-color`) unrelated to Axiom; consumer install/compile/run are clean.

**Installation matrix: PASS.**
