# Axiom 1.0 — Consumer Project Validation

*Maintainer artifact. Not shipped.* Candidate: `1.0.0-rc.1`. Five isolated audience
projects, each a fresh temp dir, `npm install` from **packed tarballs only**, `tsc`
compile against the shipped `.d.ts`, then execute. Run on Node 22.23.2 **and** Node 24.19.0
(Node 23.3.0 smoke) — every cell PASS on every Node.

| # | Project | Installs (tarballs) | Exercises | Result |
| --- | --- | --- | --- | --- |
| c1 | **consumer-app-author** | `@cynodia/axiom` | build a graph via the documented API → `validateGraph` → `compileToIR` → `createAxiomRuntime` headless → invoke actions → a constraint rolls the 4th `increment` back (final count 3) | **PASS** |
| c2 | **consumer-server-runtime** | `axiom-core` + `axiom-compiler` + `axiom-server` | `compileToServerIR` → `createAxiomServer` + `createMemoryPersistence` → drive `handle()`: a permitted action commits & advances the revision; a guard refusal returns a structured diagnostic (no throw); an admin‑only action is denied for an anonymous principal; `getState` reflects only the committed change (4 → 2) | **PASS** |
| c3 | **consumer-provider-author** | `axiom-core` + `axiom-compiler` + `axiom-server` | `FromScratchProvider implements DataProvider` — every member typed against the published interface (compile‑time proof the contract is complete); `InstrumentedProvider` composes `createMemoryDataProvider` and is registered on a real authority; a row query + an aggregate run through `handle()`; the row read‑policy isolates tenants (admin sees 3, `a1` sees 2); the custom provider is actually invoked (`query`×2, `aggregate`×1) | **PASS** |
| c4 | **consumer-agentapi-tooling** | `axiom-core` + `axiom-agent-api` | `new AgentAPI(graph)` → `inventory()` (1 action) → `explainAction` → `analyzeCapabilities` → `explainAuthorizationDecision({principal:{}})` returns `DENY` for an anonymous caller → `semanticDiff` between two graph revisions | **PASS** |
| c5 | **consumer-conformance-runner** | `@cynodia/axiom-server` only | resolve the fixture corpus **from the package's own `./conformance` export subpath** (no monorepo), run all **46** fixtures through the public `runConformanceFixture` → 46/46 pass | **PASS** |

`5 / 5` consumer projects pass on Node 22 and Node 24. `0` accidental required deep imports;
`0` dependency resolutions back into the repository in any project.

Plus the built‑in gate (`npm run release:consumer-test`, Node 24): a Counter app + orders +
patterns + authority + UI + materialization built from the 8 tarballs — **32 / 32
assertions**, and the installed `axiom` CLI (`--help`, `validate`/`explain`/`analyze`/`diff`
+ `--json`) — all green.

Sources: `axiom-tests/test-rc1/consumers/`, driver `harnesses/run-consumers.mjs`, raw
results `out/consumers-node{22,23,24}.json`.

**Consumer project validation: PASS (5/5).**
