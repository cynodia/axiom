# Axiom 1.0 — Distributed Authority Soak

*Maintainer artifact. Not shipped.* Candidate: `1.0.0-rc.1`. Spec §23–27, §29.

## Headline

| Metric | Required | Observed |
| --- | --- | --- |
| stale fenced commits | 0 | **0** |
| duplicate logical work (action / workflow / timer / effect / event) | 0 | **0** |
| lost durable work | 0 | **0** |
| authoritative stale decisions outside the contract bound | 0 | **0** (staleness‑bound‑0 preserved) |
| unauthorized live observations | 0 | **0** |
| four‑way semantic mismatches (coordination axis) | 0 | **0** |

## Multi‑authority soak (spec §23)

Elevated run: `AXIOM_AUTHORITIES=8 AXIOM_RACE_TRIALS=60 AXIOM_COHERENCE_STEPS=400
AXIOM_SQLITE_WRITERS=6 AXIOM_SQLITE_READERS=6 AXIOM_CHAOS_ITEMS=60` over
`distributed-authority-server`, `distributed-effects-race`, `distributed-scheduler-race`,
`distributed-state-coherence-race`, `eight-authority-chaos`, `external-event-dedup-race`,
`subscription-cursor-race`, `coordination-race` (bundled with the crash matrices) —
**0 failures**. Full soak tier (all files, `AXIOM_*` at 50/25) — **1707 / 1707**.

Exercised repeatedly: lease acquisition / renewal / expiry / reclaim, authority
death / restart, stale‑owner attempts, remote mutation, cache coherence, durable‑work
reclaim, event dedup across authorities, timer firing, subscription cursor progression,
live‑query resume — with randomized interleavings.

## Fencing acceptance (spec §24)

- `expired lease alone grants no authority` — `distributed/lease-fencing`,
  `probe-lease-expiry-is-not-fencing` (MATCH), `SIGSTOP the lease owner … resumed stale
  write is refused` (elevated crash matrix, pass).
- `successful newer reclaim advances generation` — `distributed/effect-reclaim`,
  `probe-lease-acquire-renew-expire` (MATCH).
- `old‑generation commits rejected` — `probe-stale-fenced-commit-rejected` (MATCH),
  `coordination-race` `spec12 §75` (elevated, pass).

**stale fenced commits = 0.**

## Logical‑work acceptance (spec §25)

`probe-two-authority-distinct-actions-equal-single`,
`probe-crash-between-actions-no-lost-committed-work`,
`probe-action-step-reconciliation-not-applied-twice`,
`probe-optimistic-conflict-no-silent-replay` — independent‑runtime one‑vs‑N topology
invariant **5/5 pass**; `spec14pt2 §3` (2 / 8 authorities claim one workflow → exactly one
logical transition) pass at elevated counts. `duplicate logical work = 0 · lost durable
work = 0`; physical attempts differ only where the contract explicitly permits.

## Revision / cache coherence (spec §26)

`distributed/cache-revision-visibility`, `probe-cache-revision-coherence-remote-commit`,
`probe-state-coherence-remote-mutation-observed` — MATCH; `distributed-state-coherence{,-race}.test.ts`,
`revision-cache{,-race}.test.ts` (elevated) — pass. The validated staleness‑bound‑0
behaviour is preserved.

## Live‑query distributed regression (spec §27)

`authorization-live-query.test.ts` (6), `authorization-pt2.test.ts` live cases,
`conformance/live/*` (13), `authorization/live-query-resume` — green. Under authority
changes: provider mutation and state mutation wake the live query; authorization is
re‑evaluated on resume; the cursor advances monotonically; a remote authority can resume.
**unauthorized live observations = 0** (`f2-state-ref-refused`, `not-live-capable` fixtures;
`authorization-pt2` live `AUTHORIZATION_DENIED` on re‑evaluation).

## Four‑way regression, coordination axis (spec §29)

`xruntime/coord-differential.mjs` — reference `runCoordinationConformanceFixture` (rc.1,
black box) vs `axiom_indep` over `conformance/distributed/*` + the `test17/corpus/c4d`
probes:

```
MATCH: 24   N1: 5   R1: 0   R2: 0   native crash: 0
```

The 5 N1 are full‑graph interleaving scripts (`distributed` kind) fed to the `coordination`
runner — both runtimes reject them identically (category mismatch, non‑semantic). The
`distributed` interleaving scripts run through `axiom_indep`'s own runner: **5/5 pass**.

`semantic(reference‑1) == semantic(reference‑N) == semantic(independent‑1) ==
semantic(independent‑N)` on the coordination / fencing / lease / effect‑claim /
event‑dedup / schedule / cursor‑fencing / cache‑coherence axis. **four‑way semantic
mismatches (coordination axis) = 0.**

> The base (non‑distributed) axis carries **2 R2 mismatches** on numeric‑conversion
> formatting — see finding **XRT‑1** in `reports/1.0-independent-runtime-regression.md` and
> `reports/1.0-findings.md`. They are independent‑runtime defects against a contract the
> reference implements correctly, not distributed‑soak findings.

**Distributed soak: PASS.**
