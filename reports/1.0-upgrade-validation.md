# Axiom 1.0 — Upgrade Validation (0.17 → 1.0)

*Maintainer artifact. Not shipped.* Candidate: `1.0.0-rc.1`. Spec §34–35.

## Headline

| Claim | Result |
| --- | --- |
| `semanticMigrationRequiredSolelyDueTo1.0Version` | **false** |
| `semanticContractChanged` | **false** |
| every 0.17‑admissible graph admits under 1.0 unchanged | **yes** |
| 0.17 persisted state / workflow / effect / event‑dedup / cursor / migration / coordination data valid under 1.0 | **yes** |

## The 0.17‑freeze → 1.0 code delta

`git diff 47df6da..ea173f5` (0.17 freeze → `1.0!!!`). The **entire** delta touching
runnable code:

| file | change |
| --- | --- |
| `packages/core/src/graph.ts` | `ApplicationGraph` constructor default `version` `'0.17.0'` → `'1.0.0'` — an authoring metadata string, non‑semantic (see §Fingerprint) |
| `packages/server/package.json`, `packages/ui-toolkit/package.json` | version + `@cynodia/*` dependency pins; `engines: {node: ">=22.0.0"}` added |
| ~140 conformance `*.json` + manifests | the `version` stamp line only (`0.17.0-alpha.1` → `1.0.0-rc.1`); every `contract: axiom.server.vN` unchanged |
| `README`, `docs/`, package READMEs, `PATTERN_CATALOG.json`, `llms.txt`, `AGENTS.md`, `reports/` | `npm run version:set` string sweep |

**No** runtime `.ts` logic changed. `SERVER_IR_LATEST_CONTRACT` still `axiom.server.v9`;
authz still `axiom.authz.v3`; `SEMANTIC_FINGERPRINT_VERSION` still `1`;
`SCHEMA_FINGERPRINT_VERSION` still `1`; protocol still `axiom.protocol.v1`.

## Fingerprint / compatibility regression (spec §34)

Verified against the rc.1 build:

| change | `semanticFingerprint` | classification |
| --- | --- | --- |
| graph `version` `'0.17.0'` vs `'1.0.0-rc.1'` vs default | **identical** (`e2f5a53…`) | version string is non‑semantic |
| presentation‑only (`textRole: title → display`) | unchanged | **COMPATIBLE** |
| action‑operation semantic (`add → subtract`) | changed | **INCOMPATIBLE** |
| constraint semantic (limit `3 → 5`) | changed | **INCOMPATIBLE** |

`Server IR == axiom.server.v9`, `authz == axiom.authz.v3`, `SEMANTIC_FINGERPRINT_VERSION`
unchanged — confirmed by direct read of the rc.1 `dist/`. The compiled Server IR
`contract` label for a given graph is identical whether the graph declares `version:
'0.17.0'` or `'1.0.0-rc.1'`. `workflow-compat.test.ts` and the `authority-identity` /
`semantic-identity` suites cover the query / workflow / provider variants — green.

## Persisted‑data continuity (spec §35)

Because the schema / fingerprint / contract versions are byte‑identical between 0.17‑frozen
and rc.1, and the persistence formats are keyed by those, 0.17‑persisted artifacts load
under rc.1 with no migration. Corroborated by: `restart.json` and
`blob-restart-preserves-references` conformance fixtures (state + blob references survive a
restart — green); `spec14pt3 §71/§104/§121` (a presentation‑only build B continues an A
workflow instance across real processes, 60 trials — green);
`schema-fingerprint-mismatch` migration fixture (a genuine schema change is *detected*,
proving the gate is live) — green.

`docs/COMPATIBILITY.md` → "Upgrading from 0.17" states this normatively and ships in the
`@cynodia/axiom` tarball.

**Upgrade validation: PASS.** Moving from the final 0.17 package set to 1.0 is a
publication, not a semantic migration.
